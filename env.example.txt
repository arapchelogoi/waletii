# ══════════════════════════════════════════════════════
#  Walletii Backend — Environment Variables
#  Copy this file to .env for local development.
#  On Render, add these in the "Environment" tab instead.
#  NEVER commit your real .env to git.
# ══════════════════════════════════════════════════════

# Your Telegram bot token from @BotFather
BOT_TOKEN=your_bot_token_here

# Your Telegram Chat ID (get from @userinfobot)
ADMIN_CHAT_ID=your_chat_id_here

# Full public URL to this server (your Render URL)
# e.g. https://walletii-backend.onrender.com
SERVER_URL=https://your-render-url.onrender.com

# Allowed origin for CORS (your frontend URL)
# Use * during development, lock it down in production
APP_URL=https://your-frontend-url.com

# Secret key for signing tokens — use any long random string
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_KEY=change_this_to_a_long_random_string

# Port (Render sets this automatically, leave as 3000 for local)
PORT=3000
